using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SFInventory
{
    public class UseItem : MonoBehaviour
    {
        // Test to see if there is item in slot.
        // on get key use item
        // Minus the item ammount
        // apply effect to healt or stamina
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    } 
}
